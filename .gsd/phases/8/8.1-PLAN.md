---
phase: 8
plan: 8.1
wave: 1
---

# Plan 8.1: Modeling PullRequestStatus

## Goal
Add the `PullRequestStatus` model to persist CI/CD signals for each pull request.

## Tasks

<task type="auto">
  <name>Add PullRequestStatus model</name>
  <files>backend/data/models.py</files>
  <action>Add PullRequestStatus class with fields: pull_request (FK), name (CharField), state (CharField), target_url (URLField), description (TextField).</action>
  <verify>Check models.py for the new class definition.</verify>
  <done>Model is defined and follows the specification.</done>
</task>

<task type="auto">
  <name>Create and run migrations</name>
  <files>backend/data/migrations/</files>
  <action>Run python manage.py makemigrations and migrate.</action>
  <verify>Check if the table exists in the database.</verify>
  <done>Migrations are applied successfully.</done>
</task>
