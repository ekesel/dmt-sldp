---
phase: 8
plan: 8.2
wave: 2
---

# Plan 8.2: Connector Expansion & Sync Expansion

## Goal
Extend the GitHub connector to fetch status checks and update the sync task to pull this data.

## Tasks

<task type="auto">
  <name>Extend GitHubConnector</name>
  <files>backend/data/connectors/github.py</files>
  <action>Implement fetch_status_checks method that uses Commit Statuses or Check Runs API.</action>
  <verify>Check github.py for the new method.</verify>
  <done>Connector can fetch CI signals.</done>
</task>

<task type="auto">
  <name>Update sync_tenant_data task</name>
  <files>backend/data/tasks.py</files>
  <action>Modify sync_tenant_data to call fetch_status_checks for active PRs and update PullRequestStatus records.</action>
  <verify>Check tasks.py for the integration logic.</verify>
  <done>Sync task now includes CI signal data.</done>
</task>
