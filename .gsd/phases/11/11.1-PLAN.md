---
phase: 11
plan: 1
wave: 1
---

# Plan 11.1: Milestone v1.2 Final Audit

## Objective
Holisitically verify all v1.2 features and ensure system stability before plateau.

## Context
- .gsd/ROADMAP.md
- .gsd/SPEC.md
- Previous Verifications (7, 8, 9, 10)

## Tasks

<task type="auto">
  <name>Verify Asynchronous Insights (Phase 7)</name>
  <files>backend/data/tasks.py</files>
  <action>
    Trigger a WorkItem update and verify that `generate_ai_insight` task is queued in Celery.
  </action>
  <verify>docker-compose logs celery | grep "generate_ai_insight"</verify>
  <done>AI insights Decoupled from main API thread.</done>
</task>

<task type="auto">
  <name>Verify CI/CD Gates (Phase 8)</name>
  <files>backend/data/engine/compliance.py</files>
  <action>
    Review logic to ensure GitHub signals are actively evaluated.
  </action>
  <verify>grep "failing_ci_checks" backend/data/engine/compliance.py</verify>
  <done>Compliance Engine successfully uses external signals.</done>
</task>

<task type="auto">
  <name>Verify Jira OAuth2 (Phase 9)</name>
  <files>backend/data/connectors/jira.py</files>
  <action>
    Confirm Bearer token usage in headers.
  </action>
  <verify>grep "Authorization: Bearer" backend/data/connectors/jira.py</verify>
  <done>Basic Auth no longer used for Jira.</done>
</task>

<task type="auto">
  <name>Verify Analytics Aggregation (Phase 10)</name>
  <files>backend/data/models.py</files>
  <action>
    Check for existing data in `DailyMetric`.
  </action>
  <verify>docker-compose run --rm backend python3 manage.py tenant_command shell --schema=test_tenant -c "from data.models import DailyMetric; print(DailyMetric.objects.count() > 0)"</verify>
  <done>Aggregated metrics persisted across schemas.</done>
</task>

## Success Criteria
- [ ] System handles background tasks without bottlenecks.
- [ ] Compliance logic correctly accounts for external CI/CD state.
- [ ] OAuth2 tokens refresh and persist.
- [ ] Aggregation provides cross-tenant visibility.
